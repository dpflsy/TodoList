<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dpflsy.todo.mapper.MemberMapper">
    <!--아이디 중복 확인-->
    <select id="getId" resultType="com.dpflsy.todo.domain.Member">
        SELECT IFNULL(COUNT(*), 0)
          FROM MEMBERS
         WHERE ID = #{id}
    </select>

    <!--회원가입-->
    <insert id="insertMember" parameterType="com.dpflsy.todo.domain.Member">
        INSERT INTO MEMBERS
                ( ID
                , PWD
                , NAME
                , EMAIL
                , AUTH)
        VALUES
                ( #{id}
                , #{pwd}
                , #{name}
                , #{email}
                , 3)
    </insert>

    <!--로그인-->
    <select id="login" resultType="com.dpflsy.todo.domain.Member">
        SELECT ID
             , NAME
             , EMAIL
             , AUTH
          FROM MEMBERS
         WHERE ID = #{id}
           AND PWD = #{pwd}
    </select>


</mapper>